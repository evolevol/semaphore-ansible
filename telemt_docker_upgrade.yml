- hosts: all
   remote_user: evol
   become: yes

   tasks:
       - name: Update telemt
          community.docker.docker_compose_v2:
               roject_src: /etc/telemt
               state: present
               pull: always
